# Задати лише якщо API на іншому домені (наприклад, окремий хост для API).
# У розробці за замовчуванням використовується http://localhost:3001; на хостингу (один сервер) — той самий хост.
# VITE_API_URL=https://api.example.com
